version: '3'

services:
  nodejs:
    build:
      dockerfile: docker/server/Dockerfile
    volumes:
      - .:/home/server/
    container_name: ${NODE_DOCKER_NAME}
    restart: always
    ports:
      - ${HOST_PORT}:${NODE_PORT}
networks:
  default:
    external:
      name: stock_network